name: Deplyment workflow2


on:
  push:
    branches:
      - test_workflow

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
        with:
          fetch-depth: 0